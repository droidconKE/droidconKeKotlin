<?xml version="1.0" encoding="utf-8"?>
<!--
  Declared here, not in :app, because this module owns the class and the firebase-messaging
  dependency. From :app, lint cannot resolve the supertype and flags Instantiatable.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <service
            android:name="com.android254.presentation.notifications.MessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>
